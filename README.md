
# Tools for GenAI

# Specific utilities:
    * `ollama`: docker compose for running ollama locally 
        * Note: will default to model appropriate for 4GB gpu
    * `notebooks`: examples of usage
        * includes example for openai (module) calling openai (api), runpod, gemini, olama
        * includes example for structured querying (openai beta using pydantic)
